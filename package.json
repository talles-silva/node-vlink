{
  "name": "node-vlink",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node build/app/cli/index.js arquivos/texto.md",
    "cli": "yarn build && yarn start",
    "cli:validate": "yarn build && yarn start --validate"
  },
  "type": "module",
  "dependencies": {
    "chalk": "^5.3.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.8.4",
    "typescript": "^5.2.2"
  }
}
